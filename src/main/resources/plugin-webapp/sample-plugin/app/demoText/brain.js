'use strict';

var fs = require('fs');
var angular = require('angular');

var template = fs.readFileSync(__dirname + '/demoText.html', 'utf8');

module.exports = function($scope, control, processData, processDiagram, Loaders, $rootScope, callbacks) {

}
